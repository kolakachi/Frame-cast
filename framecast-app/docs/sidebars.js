// @ts-check
// Sidebar auto-generates from the docs/ folder structure. Order + category
// labels are controlled by each folder's _category_.json and per-file
// sidebar_position frontmatter.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  guides: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
