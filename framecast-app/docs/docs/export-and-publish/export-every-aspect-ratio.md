---
sidebar_position: 1
title: Export every aspect ratio at once
description: One project, every format — 9:16, 1:1, 4:5, and 16:9 in a single pass.
---

# Export every aspect ratio at once

Different platforms want different shapes — Reels/TikTok (9:16), feed (1:1), portrait (4:5), YouTube (16:9). WyvStudio renders any combination from the **same** video in one export pass.

## 1. Open the export menu

In the editor, click **Export** (top right). A format menu opens.

:::info 📸 Screenshot
`img/howto/export-menu.png` — the export button with the format menu open, showing the four aspect-ratio checkboxes.
:::

## 2. Pick your formats

Tick the ratios you want — **9:16 / 1:1 / 4:5 / 16:9**. The button updates to "Export N formats."

## 3. Export

Click **Export N formats**. Each ratio queues its own render; they appear in your exports list as they finish.

:::info 📸 Screenshot
`img/howto/export-jobs.png` — the exports list showing multiple renders (one per ratio) completing.
:::

:::tip No extra credits
Each format re-frames the **same** generated assets — it doesn't regenerate any AI. So exporting all four ratios costs the same in credits as exporting one. (Exports are part of your monthly plan allowance; a batch counts each ratio against it.)
:::

:::note Framing
A scene composed for 9:16 is re-framed (cropped/letterboxed) to fill other ratios — it isn't re-composed per ratio. Keep key subjects and captions centered so they survive every crop.
:::

## Next

- [Connect a channel & publish](./connect-and-publish)
- [Share a public sample link](./share-a-sample-link)
